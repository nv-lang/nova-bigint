// `src/bigfloat/` — folder-module `bignum.bigfloat` (Nova module model: a
// folder whose co-equal `.nv` files all declare the SAME
// `module bignum.bigfloat` merge into one module). Model and naming follow
// `std/src/collections/vec/` in the `nova` repo 1:1. `_module.nv` itself
// carries no items.
//
// Layout:
//   core.nv       — `BigFloat` type, PrecisionContext/RoundingMode, arithmetic
//   core_test.nv  — unit tests
module bignum.bigfloat
